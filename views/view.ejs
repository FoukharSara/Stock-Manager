<%- include('include/_nav') %>

    <main>
        <div class="clients">
            <h2>
                <%= title %>
            </h2>
            <a href="/client_product/<%= clpr.id %>" class="adding"><i class="fa-solid fa-plus plus"></i>Ajouter Des
                Produits</a>
        </div>

        <div>
            <table class="table table-dark table-striped ">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Products</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(function(product, index) { %>
                        <tr>
                            <th scope="row">
                                <%= index + 1 %>
                            </th>
                            <td>
                                <%= product.désignation %>
                            </td>
                            <td>
                                <%= product.ClientProduct.nombre %>
                            </td>
                            <td><a href="/delete_clientproduct/<%= product.id %>"><i
                                        class="fa-solid fa-trash trash"></i></a>
                            </td>
                            
                        </tr>
                        <% }); %>
                </tbody>
            </table>
        </div>
    </main>

    <%- include('include/_footer') %>